class Solution:
    def factorial(self, N):
        n = 1
        for i in range(1,N+1):
            n = n * i
        
        arr = []
        while n>0:
            rem = n % 10
            arr.insert(0,rem)
            n = n // 10
            
        return arr
