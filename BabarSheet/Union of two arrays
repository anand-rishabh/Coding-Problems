class Solution:    
    #Function to return the count of number of elements in union of two arrays.
    def doUnion(self,a,n,b,m):
        
        st = []
        for i in range(n):
            st.append(a[i])
        for j in range(m):
            st.append(b[j])
        st = set(st)
        return len(st)
